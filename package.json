{
  "name": "@floating-live/plugin-tools",
  "type": "module",
  "version": "0.1.0-alpha.1",
  "private": false,
  "scripts": {
    "clear": "rimraf ./bin",
    "build": "npm run clear && tsc --build"
  },
  "dependencies": {
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "@types/node": "^25.6.2",
    "rimraf": "^6.1.3",
    "typescript": "^6.0.3"
  },
  "bin": {
    "floating-live-plugin-tools": "./bin/index.js"
  },
  "files": [
    "./bin"
  ]
}
